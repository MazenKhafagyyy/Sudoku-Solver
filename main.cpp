#include <iostream>
#include "matrix.h"

using namespace std;

Matrix solver(Matrix sudoku)
{
    Matrix solution;

    return solution;
}

int main() {

    Matrix A(9,9,0.0);
    bool run = true;

    while (run)
    {
        cin>>A;
    }

    return 0;
}
